import styled from "styled-components";

const Map = styled.div`
  position: relative;
  width: 100%;

  iframe {
    width: 100%;
    height: 300px;
    border: none;
  }
`

export const S = {
    Map
}